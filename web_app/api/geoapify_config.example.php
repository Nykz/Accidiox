<?php
// Copy this file to geoapify_config.php and fill in your real key —
// geoapify_config.php itself is gitignored (never committed) because it
// holds a live API key once deployed. Get a free key at
// myprojects.geoapify.com (no credit card needed): create a project, and
// it auto-generates an API key on the "API keys" page.

$geoapify_api_key = "your_geoapify_api_key_here";
?>
