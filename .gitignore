# OS / editor noise
.DS_Store
Thumbs.db
*.log

# Node (only used locally for the http-server preview task)
node_modules/

# Exact duplicate of PROJECT_MASTER_DOCUMENTATION.md — the .md is the
# canonical copy tracked in this repo (renders nicely on GitHub).
PROJECT_MASTER_DOCUMENTATION.txt

# Real production DB credentials live ONLY on the server (set directly via
# SSH), never in git — this repo is public. Gitignoring this specific path
# also makes Hostinger's git auto-deploy PRESERVE the server's live copy
# instead of overwriting it with whatever placeholder sits in git on every
# push (verified: an earlier push without this wiped the live credentials).
web_app/api/db_config.php

# Same reasoning as db_config.php above, for the Meta WhatsApp Cloud API
# access token.
web_app/api/meta_whatsapp_config.php

# Same reasoning again, for the Geoapify API key.
web_app/api/geoapify_config.php

# Raw Sketchfab download (model + zip) — the actual copy used by the app
# lives at web_app/assets/models/yamaha_r1.glb, which IS tracked.
/3d models/

# PWABuilder-generated Android package: contains signing.keystore + its
# plaintext password in signing-key-info.txt. This repo is public — never
# commit the keystore or its password, or anyone could sign a fake "update"
# to this app using our identity. Keep these files only on your own machine.
/apk_package/
/apk_package_crew/
/*.apk
/*.aab
/Accidiox - Google Play package.zip

# Same reasoning as db_config.php: Twilio auth token and SMTP credentials.
web_app/api/twilio_voice_config.php
web_app/api/email_config.php

# Runtime state written by the PHP APIs (schema flags, old dispatch JSON).
web_app/api/data/

# Local pitch-deck workshop: generated .pptx files, their build scripts and
# raw capture folders. The deck that matters is exported to PDF and uploaded
# to the submission form, so none of this belongs in the repo.
/*.pptx
/build_*.js
/make_pptx.js
/test_deck.html
/ppt prompt.txt
/KAYA_IIT_Varanasi_Pitch_Deck_20_Slides.md
/screens/
/accident images/
/accident videos/

# Superseded by docs/Accidiox_Demo_Video_and_Pitch_Guide.*
/Accidiox_Video_Script_and_Presentation_Guide.html
/Accidiox_Video_Script_and_Presentation_Guide.pdf

# Local working files, not part of the project:
# AI/pitch prompts, presentation guides, deck outlines and the little
# scripts that render them. The repository holds the product only.
/.claude/
/tools/
/package.json
/package-lock.json
docs/Accidiox_Demo_Video_and_Pitch_Guide.*
docs/Accidiox_PPT_Prompt*
docs/kaya_slide_deck_outline.md
docs/KAYA_IIT_Varanasi_Pitch_Deck_20_Slides.md
/generate_pitch_deck_kaya.js
docs/Accidiox_Judges_Quick_Guide.*
